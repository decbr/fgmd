// GitHub's full :shortcode: table (about 1,900 entries), kept out of the main entry so you only
// ship it if you use it:
//
//   import { emoji } from 'fgmd';
//   import { gemoji } from 'fgmd/emoji';
//   markdown(source, { plugins: [emoji({ map: gemoji })] });
export { gemoji } from './emoji-data.js';
